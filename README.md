# LATTICCE — workspace reset

This repository was reset as requested. Current main shows a minimal page with the word "LATTICCE" on a black background.

A backup of the previous workspace is available in the branch `pre-wipe`.

Next steps: push to GitHub (provide remote or run git remote add origin <url> && git push -u origin main).