2026-03-28 — Resumen de eventos duraderos

- Usuario: Dann (prefiere español). Persona: autorizó múltiples cambios en el repo Uroboros durante la sesión.

- Tarea principal: reconstruir y publicar el sitio Uroboros usando la imagen de referencia "Home" en workspace/Recursos y el contenido del Word "Contenido.docx". El manual de marca fue provisto en Recursos/Manual de Marca - LATTICE.pdf y se usó como guía estética.

- Decisiones y permisos del usuario:
  - Confirmó usar el contenido del Word ("Confirmo contenido").
  - Solicitó publicar inmediatamente en GitHub Pages ("publicar ahora").
  - Autorizó pushes/commits para crear ramas y actualizar workflows (varias autorizaciones a lo largo de la sesión).
  - Autorizó la creación de una rama de reconstrucción (rebuild/home / fresh/home) y luego pidió borrar lo anterior y empezar de cero; solicitó que se borrara también la rama gh-pages.

- Acciones realizadas por el asistente (resumen):
  - Exploración y lectura de archivos en workspace/Recursos: Contenido.docx (texto extraído), Home.jpeg (imagen usada), Manual de Marca - LATTICE.pdf (guía de estilo).
  - Creó ramas de trabajo (feature/home-react, rebuild/home, fresh/home) para implementar la nueva home en React (Vite). Añadió scaffold de React, componentes y estilos.
  - Añadió y subió la imagen de referencia al repo (Recursos/Home.jpeg).
  - Implementó workflows de GitHub Actions para construir y desplegar a gh-pages; inicialmente usó actions/configure-pages + upload-pages-artifact + deploy-pages, pero el workflow falló por dependencia obsoleta (upload-artifact v3).
  - Actualizó el workflow: forzó Node24 para acciones JS y luego reemplazó el flujo por un deploy manual (clonar/crear gh-pages, copiar dist, push con GITHUB_TOKEN) para evitar la dependencia obsoleta.
  - El usuario accidentalmente pegó un GitHub PAT en el chat; el asistente pidió revocarlo y solicitó al usuario que lo revocara inmediatamente (usuario confirmó que revocó el token). El asistente no retuvo ni usó el token.
  - El asistente eliminó las ramas de trabajo y la rama gh-pages a pedido del usuario ("si borralo" → borró feature/home-react, rebuild/home y gh-pages tanto remoto como local).
  - Reconstruyó desde cero en branch fresh/home, subió código y workflow. Al final ajustó el workflow para crear una rama gh-pages orphan cuando la rama no existe y empujar los artefactos del build.
  - Se disparó la Action de despliegue; hubo un fallo inicial por la versión obsoleta de upload-artifact v3; el asistente actualizó el workflow a un deploy manual y reintentó. El run actual se encuentra en progreso al cierre de esta nota.

- Preferencias técnicas observadas:
  - Stack elegido por el usuario: React (Vite). 
  - Estética: paleta y estilo extraídos de la imagen Home (aprox: #0A0A0A fondo, #EAF6FF luz/claros, #4EC8FF acento, #9B7CFF 2º acento). Tipografía recomendada: geométrica/neo-grotesca (Inter usada como fallback).
  - Animaciones: el usuario pidió versión animada (scroll reveal, partículas/efectos suaves en hero).

- Qué esperar/o próximo paso automático:
  - Finalizar el run de GitHub Actions; si concluye con éxito el contenido de ./dist será empujado a gh-pages y la URL https://dannlagger.github.io/Uroboros/ estará disponible (puede tardar minutos en propagarse).

Notas de seguridad / límites
- El token que el usuario pegó fue solicitado que lo revoque; usuario confirmó revocación. El token no fue usado ni almacenado.

Si quieres que archive más detalles (logs, commits específicos, hashes), dime y los agrego a esta entrada.
